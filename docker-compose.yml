services:
  caddy:
    image: caddy:alpine
    restart: unless-stopped
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ./docs:/srv

      - caddy_data:/data
      - caddy_config:/config

volumes:
  caddy_data:
  caddy_config:
